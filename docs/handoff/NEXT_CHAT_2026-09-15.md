# Next chat: start here (written 2026-09-15, for the phone debug session)

**Paste this to open the new chat:**

```
Read docs/handoff/NEXT_CHAT_2026-09-15.md first. Phone debug: GPS Transistor
at bakeoff-3.
```

---

## 1. What the new chat is for

The 5-minute plug-in retest of **GPS Transistor** at build `bakeoff-3`, which
fixes the reopen bug found this morning. His words on it: *"fix the reopen
bug. I will let you test after work if I am not in a terrible mood which is
very likely the way this morning is going"*.

**On his phone, when he says he is ready:** Auto Blocker off, unlock, plug in,
leave it on the desk. Then, from the desktop, with `-Serial RFGL4275NVH` on
every task:

1. `.\android\bakeoff\bakeoff.ps1 -Task setup -Serial RFGL4275NVH` - installs
   bakeoff-3 over the top (same application ids, so sessions and grants
   survive) and reads every grant back.
2. `... -Task start -Serial RFGL4275NVH -Only T` - GPS Transistor alone.
   GPS Custom's wake lock keeps the phone awake for both, which is what hid
   the thing being tested (Fable, `docs/handoff/REPORT_2.3.md`, Section 9).
3. Drive the bug from the desktop: recent apps, **Close all**, reopen GPS
   Transistor, then watch `... -Task status -Serial RFGL4275NVH -Only T`.
   **Pass = `fixes.csv` keeps growing after the reopen**, and the event log
   has `sdk_listeners` at the resume.
4. If he leaves it recording and locked by a window for another 15 minutes,
   that is a second locked-and-still reading. Then `-Task stop ... -Only T`,
   `-Task pull ...`, and score.
5. Prompt him to turn Auto Blocker back on when the phone work is done.

Never uninstall either test app while an export it made is still on the phone
and unpulled (Fable's export trap, `REPORT_2.3.md` Section 8).

## 2. Where everything stands

- **golf-tracker v25 is live and hash-verified** (`a12570a`, pushed
  2026-09-15; the served `build.js` and `sw.js` match the commit byte for
  byte). **His phone check is still owed:** Settings shows `Build v25`, then
  MARK TEE SHOT, MARK SHOT 2, and all six lie buttons must be on screen
  without scrolling. If the bottom row is cut off, v25 stays off the course
  and the numbers come back here.
- **Fable's queue is empty.** 2.2 (v24 FAIL), 2.3 (the test apps PASS), 2.4
  (v25 PASS) and 1.1 (the course map) are all answered; lines in
  `docs/handoff/FOR_FABLE_LOG.md`, reports beside it.
- **Test apps:** GPS Custom and GPS Transistor, build `bakeoff-3` built on
  this PC, **not yet installed on the phone** (the phone has bakeoff-2).
- **This morning's locked test** (session `20260915-075447-T`, n = 1): the
  SDK's own store passed (0 gaps, longest 12 s); the app log lost 342 s after
  a Close all and reopen - the bug now fixed. Locked and still for 15 min, it
  logged a fix in 37% of seconds, longest wait 11.5 s, no gap over 20 s. Data
  is in `docs/roundDownloads/bakeoff/20260915-085908/` (gitignored, not
  committed).
- **Git:** `main` is ahead of `origin/main` by docs, notes, the bake-off apps
  and the rules work. Nothing the phone runs is unpushed. The push is the
  deploy and runs only on his word.

## 3. Open, his call

1. **The locked-screen carry** for the bar (screen locked, music playing):
   a desk-started walk, or a round with golf-tracker closed. Not chosen.
2. **The poor-accuracy warning** now appears after the lie tap, not before
   (`docs/handoff/REPORT_2.4.md`, Section 4). His preference, not a defect.
3. **The course map as app data** is a schema change (Fable 1.1) and xhigh on
   his word. Opus's recommendation, 2026-09-15: not now - it gains nothing
   this week and belongs with the native storage design. His question that
   settled it: *"What is this gaining right now. I need to be careful with my
   Fable usage"*.
4. **Typed first-putt threshold** (15 ft or 20 ft) is still open
   (`docs/HANDOFF-native-build.md`, Section 4, item 3).

## 4. Rules that changed today

- **Global CLAUDE.md Section 4 was revised 2026-09-15** (the Fable effort
  ladder, the 15% weekly ceiling, who writes Fable-owned code). This repo's
  part is done: `.claude/agents/fable-low.md` and `fable-medium.md` exist,
  and the ladder and rule 4 are in `CLAUDE.md` Section 1. Cross-repo rule
  documents live in `C:\Temp\gitRepos\rules` - read only from here.
- **A test he has to carry out leads with the minimum that still tells us
  something.** His words, 2026-09-15: *"2 hours - get real. What is the
  minimum time you need. I can give like 15-30 minutes this morning"*.

## 5. Gotchas already paid for

- **adb needs Auto Blocker off.** Prompt him to put it back on with one plain
  question when the phone work is done.
- **An emulator from another session** (`emulator-5554`) is usually attached;
  pass `-Serial RFGL4275NVH` to every `bakeoff.ps1` task and leave it alone.
- **`pull` prints its success line on stderr**, so PowerShell can report the
  task as failed when it worked. Check the pulled folder before believing it.
- **Serve with `preview_start` name `golf-tracker`**; the suite is
  `http://localhost:8123/test/` (511/511 at v25). Never `python -m http.server`.
- **Git:** stage by name, never `git add -A`; commits are rusty9645@gmail.com.
